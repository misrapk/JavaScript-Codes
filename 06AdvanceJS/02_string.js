//TODO: use backticks (``) to define strings

var name = `peeyush`;
var fullName = `${name} Kant Misra`;

console.log(fullName);
console.log(fullName.charAt(0)); //character at 0th position
console.log(fullName.endsWith("?")); //true if ends with ? else fast
console.log(fullName.toUpperCase()); //change to upper case